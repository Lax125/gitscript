[build-system]
requires = ["setuptools>=61", "pyreadline3"]
build-backend = "setuptools.build_meta"

[project]
name = "gitscript"
version = "0.1.0"
dependencies = ["pyvis"]

[project.scripts]
gitscript = "gitscript.main:main"

[tool.setuptools.packages.find]
where = ["src"]
